spring.application.name=ShoeShopBackEnd

# MariaDB
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/shoeshop}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# JWT config
jwt.secret=${JWT_SECRET:0123456789012345678901234567890123456789012345678901234567890123_na_na_na_na_anh_phung_thanh_do}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:trungquyen1502@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jwur ftbs wiwn xedb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# AWS S3 Configuration
aws.s3.bucket=${AWS_S3_BUCKET:}
aws.s3.region=${AWS_S3_REGION:}
aws.accessKey=${AWS_ACCESS_KEY:}
aws.secretKey=${AWS_SECRET_KEY:}
